apiVersion: v1
kind: Secret
metadata:
  name: {{ include "bijmantra-backend.fullname" . }}-secrets
  labels:
    {{- include "bijmantra-backend.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $val := .Values.secrets }}
  {{ $key }}: {{ $val | b64enc | quote }}
  {{- end }}
