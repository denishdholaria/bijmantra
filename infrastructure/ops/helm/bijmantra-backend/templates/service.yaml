apiVersion: v1
kind: Service
metadata:
  name: {{ include "bijmantra-backend.fullname" . }}
  labels:
    {{- include "bijmantra-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bijmantra-backend.selectorLabels" . | nindent 4 }}
