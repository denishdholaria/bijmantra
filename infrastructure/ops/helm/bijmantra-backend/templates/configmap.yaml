apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bijmantra-backend.fullname" . }}-config
  labels:
    {{- include "bijmantra-backend.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
