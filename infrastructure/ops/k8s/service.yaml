apiVersion: v1
kind: Service
metadata:
  name: bijmantra-backend-service
spec:
  selector:
    app: bijmantra-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: ClusterIP
